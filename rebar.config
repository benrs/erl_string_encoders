{plugins, [pc]}.

{provider_hooks,
 [
  {pre,
   [
    {compile, {pc, compile}},
    {clean, {pc, clean}}
   ]
  }
 ]
}.

{port_specs, [{"priv/erl_string_encoders_nif.so", ["c_src/*.c"]}]}.

{port_env, [{"LDFLAGS",
             "$LDFLAGS -lmodpbase64"}]}.
